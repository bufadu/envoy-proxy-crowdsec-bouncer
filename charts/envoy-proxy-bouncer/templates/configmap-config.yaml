apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "envoy-proxy-bouncer.fullname" . }}-config
  labels:
    {{- include "envoy-proxy-bouncer.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- include "envoy-proxy-bouncer.cleanConfig" .Values.config | nindent 4 }}
